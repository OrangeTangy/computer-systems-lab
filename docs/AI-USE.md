# AI-use disclosure

OpenAI Codex assisted with assignment interpretation, benchmark implementation, experiment scheduling, validation, plots, and explanatory writing. Codex executed the measurements retained in this repository. No numerical data was invented. Hypotheses are retained separately from observations.

The user selected the initial career direction and then requested all three projects in a shared GitHub repository. The prior memory-only work is preserved in Project 2. The additional suite is based on the same connected i5-1240P Windows machine, not an assumed new computer.

Independent student work is **not yet documented**. Before submission, the student should understand each kernel, reproduce selected trials, explain at least one anomaly, and record actual modifications, tests or rejected AI suggestions. Do not claim AI-executed tests as independent student validation.

Important rejected assumptions: wider vector instructions always improve runtime; sequential-chain timing equals intrinsic DRAM latency; a 4 KiB page-spacing slowdown proves TLB misses; requested SSD queue depth equals achieved queue depth; a short storage run establishes steady state.

Lecture integration: Codex visually reviewed all 48 pages across the three user-supplied Thread 1-1 decks, wrote original explanations with page references, and added a controlled memory follow-up prompted by the placement/concurrency concepts. The original slides were not uploaded to the public repository. An initial text extraction revealed image-based slides, so visual review was necessary. Codex also prepared an administrator-run counter collector; a user statement that Administrator PowerShell is available is not recorded as a completed independent experiment.
