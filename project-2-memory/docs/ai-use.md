# AI-use disclosure (revise honestly before submission)

OpenAI Codex helped interpret the assignment, design initial experiments, generate the C benchmark and Python run/analysis scripts, and draft learning/methodology notes. Codex executed the recorded Windows validation and pilot runs and inspected their outputs. Raw data is from actual executions, not invented values.

Student independent modifications, tests, rejected suggestions, and explanations: not yet recorded. Add only work you actually perform. Do not claim AI-executed checks as independent student validation. Review each algorithm, reproduce selected measurements, explain discrepancies, and record changes here before submission.

An early PDF rendering attempt used an unavailable Python package; the PDF was then rendered successfully with an available library. Windows blocked some hardware queries and WSL enumeration, so the preliminary environment record marks unverified settings explicitly.
